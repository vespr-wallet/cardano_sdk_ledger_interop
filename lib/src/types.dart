// Base58 for Byron addresses | Bech32 for Shelley addresses
typedef Bech32OrBase58CardanoAddress = String;
// In the format `${txHash}#${index}`
typedef UtxoAndIndex = String;

typedef CredentialsHex = String;
